'use client';
// src/lib/langgraph/runtime.ts
// Stubbed for v1 deploy — LangGraph runtime wired in v2

export function useBidDeedRuntime() {
  // Returns a minimal runtime stub — full LangGraph integration in v2
  return null as unknown as ReturnType<typeof useBidDeedRuntime>;
}
